<?xml version="1.0" encoding="utf-8"?>
<search> 
  
    
    <entry>
      <title><![CDATA[go module 初步认知]]></title>
      <url>/2019/02/24/go-module-md/</url>
      <content type="html"><![CDATA[<h2 id="GOPATH"><a href="#GOPATH" class="headerlink" title="GOPATH"></a>GOPATH</h2><p>在 go1.11 之前，不是使用godep glide，就是直接使用go get安装第三方包</p>
<p>工作空间Workspaces，是Go项目的根目录，也就是<code>GOPATH</code> 是GO项目必备的环境变量，用来存放Go的开发代码和第三方包代码，代码需要按照一定的目录安排</p>
<h2 id="mod-特性"><a href="#mod-特性" class="headerlink" title="mod 特性"></a>mod 特性</h2><h3 id="模块定义："><a href="#模块定义：" class="headerlink" title="模块定义："></a>模块定义：</h3><p>模块根目录和其子目录的所有包构成模块，在根目录下存在 go.mod 文件，子目录会向着父目录、爷目录一直找到 go.mod 文件</p>
<p>环境变量 GO111MODULE 开启或关闭模块支持</p>
<ul>
<li>GO111MODULE=off 无模块支持，go 会从 GOPATH 和 vendor 文件夹寻找包。</li>
<li>GO111MODULE=on 模块支持，go 会忽略 GOPATH 和 vendor 文件夹，只根据 go.mod 下载依赖。</li>
<li>GO111MODULE=auto 在 <code>$GOPATH/src</code> 外面且根目录有 go.mod 文件时，开启模块支持</li>
</ul>
<p>go.mod 可以用来定义当前模块依赖和版本，也可以用来替换和排除依赖</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line">// 定义的模块名称</div><div class="line">module example.com/m</div><div class="line"></div><div class="line">// 模块依赖</div><div class="line">require (</div><div class="line">    golang.org/x/text v0.3.0</div><div class="line">    gopkg.in/yaml.v2 v2.1.0</div><div class="line">)</div><div class="line"></div><div class="line">// 有时由于网络原因，golang.org/x下的包加载不到，可以使用github.com下的包替代</div><div class="line">replace (</div><div class="line">    golang.org/x/text =&gt; github.com/golang/text v0.3.0</div><div class="line">)</div></pre></td></tr></table></figure>
<ul>
<li>require语句指定的依赖项模块</li>
<li>replace语句可以替换依赖项模块</li>
<li>exclude语句可以忽略依赖项模块</li>
</ul>
<h3 id="常用命令"><a href="#常用命令" class="headerlink" title="常用命令"></a>常用命令</h3><ul>
<li>go mod init:初始化modules</li>
<li>go mod download:下载modules到本地cache</li>
<li>go mod edit:编辑go.mod文件，选项有-json、-require和-exclude，可以使用帮助go help mod edit</li>
<li>go mod graph:以文本模式打印模块需求图</li>
<li>go mod tidy:删除错误或者不使用的modules</li>
<li>go mod vendor:生成vendor目录</li>
<li>go mod verify:验证依赖是否正确</li>
<li>go mod why：查找依赖</li>
</ul>
<p>go build -getmode=vendor 在开启模块支持的情况下，用这个可以退回到使用 vendor 的时代</p>
<h3 id="go的-mod与get"><a href="#go的-mod与get" class="headerlink" title="go的 mod与get"></a>go的 mod与get</h3><pre><code>go get 命令会将文件下载到$GOPATH/src 下的相应目录
go mod 会把文件下载到$GOPATH/pkg/mode 目录下，供其他项目共享
</code></pre><p>go get命令也与时俱进，支持了modules</p>
<ul>
<li>运行 go get -u 将会升级到最新的次要版本或者修订版本</li>
<li>运行 go get -u=patch 将会升级到最新的修订版本（比如说，将会升级到 1.0.1 版本，但不会升级到 1.1.0 版本）</li>
<li>运行 go get package@version将会升级到指定的版本号</li>
</ul>
<h2 id="如何创建go-module项目"><a href="#如何创建go-module项目" class="headerlink" title="如何创建go module项目"></a>如何创建go module项目</h2><ol>
<li>定义入口文件</li>
</ol>
<figure class="highlight go"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div></pre></td><td class="code"><pre><div class="line"><span class="comment">// package main 后一定要跟import 注释的内容就是模块的名称</span></div><div class="line"><span class="keyword">package</span> main <span class="comment">// import "github.com//xxx/examples/modules"</span></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</div><div class="line">    <span class="comment">// ...</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<ol>
<li>在项目目录下执行初始化操作</li>
</ol>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">$ go mod init</div><div class="line">go: creating new go.mod: module github.com/xxx/examples/modules</div></pre></td></tr></table></figure>
<pre><code>TIP: 不能在GOPATH 所在的目录下创建mod，不然会报异常
</code></pre><ol>
<li>编译执行,查看是否正常</li>
</ol>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">$ go build .</div><div class="line">$ ./modules</div></pre></td></tr></table></figure>
]]></content>
      
        
        <tags>
            
            <tag> golang </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[一个小脚本引发的点滴记录]]></title>
      <url>/2018/05/02/shell_first/</url>
      <content type="html"><![CDATA[<h2 id="zsh自定义脚本"><a href="#zsh自定义脚本" class="headerlink" title="zsh自定义脚本"></a>zsh自定义脚本</h2><p>在 zsh脚本中添加自定义的快捷方式, 脚本内容如下：<br><figure class="highlight sh"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">function</span> dkexec &#123;</div><div class="line">    <span class="keyword">if</span> [ -z <span class="string">"<span class="variable">$1</span>"</span> ]; <span class="keyword">then</span></div><div class="line">        <span class="built_in">echo</span> <span class="string">"Usage: dockerexec mysql|redis|..."</span></div><div class="line">    <span class="keyword">else</span></div><div class="line">        image=`docker ps | grep <span class="variable">$1</span> | awk <span class="string">'&#123;print $1&#125;'</span>`</div><div class="line">        <span class="keyword">if</span> [ -z <span class="string">"<span class="variable">$image</span>"</span> ]; <span class="keyword">then</span></div><div class="line">            <span class="built_in">echo</span> <span class="string">"can't find image by name <span class="variable">$1</span>"</span></div><div class="line">        <span class="keyword">else</span></div><div class="line">            docker <span class="built_in">exec</span> -it <span class="variable">$&#123;image&#125;</span> bash</div><div class="line">        <span class="keyword">fi</span></div><div class="line">    <span class="keyword">fi</span></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p><strong>遇到的问题：</strong></p>
<ol>
<li><p>判断语句与括号间需要空格分割</p>
 <figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">if [ -z &quot;$1&quot;] 如果$1所在的位置与]之间没有空格，那么会报错误 “dkexec:[:1: &apos;]&apos; expected” </div><div class="line"></div><div class="line">如果 $1为空，那么就变成了 if[ -z ], 会把]当成一个字符，引发异常</div></pre></td></tr></table></figure>
</li>
<li><p>所有的if都需要相应的fi来进行闭合</p>
</li>
<li><p>在使用awk的print方法时，如果保证获取的结果值可以赋值给结果变量？</p>
 <figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">需要将语句用反引号``包括起来，才能将结果值赋予image</div></pre></td></tr></table></figure>
</li>
</ol>
<h2 id="引申的知识点"><a href="#引申的知识点" class="headerlink" title="引申的知识点"></a>引申的知识点</h2><h3 id="0-1-2-变量的含义"><a href="#0-1-2-变量的含义" class="headerlink" title=" $0, $1, $2 ...变量的含义"></a><strong> <code>$0, $1, $2 ...</code>变量的含义</strong></h3><table>
<thead>
<tr>
<th>变量</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td><code>$0</code></td>
<td>当前脚本的文件名</td>
</tr>
<tr>
<td><code>$n</code></td>
<td>传递给脚本或函数的参数。n 是一个数字，表示第几个参数。例如，第一个参数是<code>$1</code>，第二个参数是<code>$2</code>。</td>
</tr>
<tr>
<td><code>$#</code></td>
<td>传递给脚本或函数的参数个数。</td>
</tr>
<tr>
<td><code>$*</code></td>
<td>传递给脚本或函数的所有参数。</td>
</tr>
<tr>
<td><code>$@</code></td>
<td>传递给脚本或函数的所有参数。被双引号(“ “)包含时，与 <code>$*</code> 稍有不同，下面将会讲到。</td>
</tr>
<tr>
<td><code>$?</code></td>
<td>上个命令的退出状态，或函数的返回值。</td>
</tr>
<tr>
<td><code>$$</code></td>
<td>当前Shell进程ID。对于 Shell 脚本，就是这些脚本所在的进程ID。</td>
</tr>
</tbody>
</table>
<pre><code>`$*` 和 `$@` 的区别

    `$*` 和 `$@` 都表示传递给函数或脚本的所有参数，不被双引号(&quot; &quot;)包含时，都以&quot;`$1`&quot; &quot;`$2`&quot; … &quot;`$n`&quot; 的形式输出所有参数

    但是当它们被双引号(&quot; &quot;)包含时，&quot;`$*`&quot; 会将所有的参数作为一个整体，以&quot;`$1` `$2` … `$n`&quot;的形式输出所有参数；&quot;`$@`&quot; 会将各个参数分开，以&quot;`$1`&quot; &quot;`$2`&quot; … &quot;`$n`&quot; 的形式输出所有参数。
</code></pre><h3 id="z-a-等判断条件的使用"><a href="#z-a-等判断条件的使用" class="headerlink" title="-z -a ...等判断条件的使用"></a><strong><code>-z -a ...</code>等判断条件的使用</strong></h3><pre><code>- 字符串判断
    str1 = str2　　　　　　当两个串有相同内容、长度时为真
    str1 != str2　　　　　 当串str1和str2不等时为真
    -n str1　　　　　　　 当串的长度大于0时为真(串非空)
    -z str1　　　　　　　 当串的长度为0时为真(空串)
    str1　　　　　　　　   当串str1为非空时为真

- 数字的判断
    int1 -eq int2　　　　两数相等为真
    int1 -ne int2　　　　两数不等为真
    int1 -gt int2　　　　int1大于int2为真
    int1 -ge int2　　　　int1大于等于int2为真
    int1 -lt int2　　　　int1小于int2为真
    int1 -le int2　　　　int1小于等于int2为真

- 文件的判断
    -r file　　　　　用户可读为真
    -w file　　　　　用户可写为真
    -x file　　　　　用户可执行为真
    -f file　　　　　文件为正规文件为真
    -d file　　　　　文件为目录为真
    -c file　　　　　文件为字符特殊文件为真
    -b file　　　　　文件为块特殊文件为真
    -s file　　　　　文件大小非0时为真
    -t file　　　　　当文件描述符(默认为1)指定的设备为终端时为真

- 复杂逻辑判断
    -a 　 　　　　　 与
    -o　　　　　　　 或
    !　　　　　　　　非
</code></pre><p>附表：</p>
<table>
<thead>
<tr>
<th>语法</th>
<th>含义</th>
</tr>
</thead>
<tbody>
<tr>
<td>[ -a FILE ]</td>
<td>如果 FILE 存在则为真。</td>
</tr>
<tr>
<td>[ -b FILE ]</td>
<td>如果 FILE 存在且是一个块特殊文件则为真。</td>
</tr>
<tr>
<td>[ -c FILE ]</td>
<td>如果 FILE 存在且是一个字特殊文件则为真。</td>
</tr>
<tr>
<td>[ -d FILE ]</td>
<td>如果 FILE 存在且是一个目录则为真。</td>
</tr>
<tr>
<td>[ -e FILE ]</td>
<td>如果 FILE 存在则为真。</td>
</tr>
<tr>
<td>[ -f FILE ]</td>
<td>如果 FILE 存在且是一个普通文件则为真。</td>
</tr>
<tr>
<td>[ -g FILE ]</td>
<td>如果 FILE 存在且已经设置了SGID则为真。 [ -h FILE ]  如果 FILE 存在且是一个符号连接则为真。</td>
</tr>
<tr>
<td>[ -k FILE ]</td>
<td>如果 FILE 存在且已经设置了粘制位则为真。</td>
</tr>
<tr>
<td>[ -p FILE ]</td>
<td>如果 FILE 存在且是一个名字管道(F如果O)则为真。</td>
</tr>
<tr>
<td>[ -r FILE ]</td>
<td>如果 FILE 存在且是可读的则为真。</td>
</tr>
<tr>
<td>[ -s FILE ]</td>
<td>如果 FILE 存在且大小不为0则为真。</td>
</tr>
<tr>
<td>[ -t FD ]</td>
<td>如果文件描述符 FD 打开且指向一个终端则为真。</td>
</tr>
<tr>
<td>[ -u FILE ]</td>
<td>如果 FILE 存在且设置了SUID (set user ID)则为真。</td>
</tr>
<tr>
<td>[ -w FILE ]</td>
<td>如果 FILE 如果 FILE 存在且是可写的则为真。</td>
</tr>
<tr>
<td>[ -x FILE ]</td>
<td>如果 FILE 存在且是可执行的则为真。</td>
</tr>
<tr>
<td>[ -O FILE ]</td>
<td>如果 FILE 存在且属有效用户ID则为真。</td>
</tr>
<tr>
<td>[ -G FILE ]</td>
<td>如果 FILE 存在且属有效用户组则为真。</td>
</tr>
<tr>
<td>[ -L FILE ]</td>
<td>如果 FILE 存在且是一个符号连接则为真。</td>
</tr>
<tr>
<td>[ -N FILE ]</td>
<td>如果 FILE 存在 and has been mod如果ied since it was last read则为真。</td>
</tr>
<tr>
<td>[ -S FILE ]</td>
<td>如果 FILE 存在且是一个套接字则为真。</td>
</tr>
<tr>
<td>[ FILE1 -nt FILE2 ]</td>
<td>如果 FILE1 has been changed more recently than FILE2, or 如果 FILE1 exists and FILE2 does not则为真。</td>
</tr>
<tr>
<td>[ FILE1 -ot FILE2 ]</td>
<td>如果 FILE1 比 FILE2 要老, 或者 FILE2 存在且 FILE1 不存在则为真。</td>
</tr>
<tr>
<td>[ FILE1 -ef FILE2 ]</td>
<td>如果 FILE1 和 FILE2 指向相同的设备和节点号则为真。</td>
</tr>
<tr>
<td>[ -o OPTIONNAME ]</td>
<td>如果 shell选项 “OPTIONNAME” 开启则为真。</td>
</tr>
<tr>
<td>[ -z STRING ]</td>
<td>“STRING” 的长度为零则为真。</td>
</tr>
<tr>
<td>[ -n STRING ] or [ STRING ]</td>
<td>“STRING” 的长度为非零 non-zero则为真。</td>
</tr>
<tr>
<td>[ STRING1 == STRING2 ]</td>
<td>如果2个字符串相同。 “=” may be used instead of “==” for strict POSIX compliance则为真。</td>
</tr>
<tr>
<td>[ STRING1 != STRING2 ]</td>
<td>如果字符串不相等则为真。</td>
</tr>
<tr>
<td>[ STRING1 <code>&lt;</code> STRING2 ]</td>
<td>如果 “STRING1” sorts before “STRING2” lexicographically in the current locale则为真。</td>
</tr>
<tr>
<td>[ STRING1 &gt; STRING2 ]</td>
<td>如果 “STRING1” sorts after “STRING2” lexicographically in the current locale则为真。</td>
</tr>
<tr>
<td>[ ARG1 OP ARG2 ]</td>
<td>“OP” is one of -eq, -ne, -lt, -le, -gt or -ge. These arithmetic binary operators return true if “ARG1” is equal to, not equal to, less than, less than or equal to, greater than, or greater than or equal to “ARG2”, respectively. “ARG1” and “ARG2” are integers.</td>
</tr>
</tbody>
</table>
]]></content>
      
        
        <tags>
            
            <tag> shell </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[java同步器AQS的独享模式分析]]></title>
      <url>/2018/04/17/aqs-exclusive/</url>
      <content type="html"><![CDATA[<h2 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h2><p>在java.util.concurrent包中有很多控制同步和并发的类，其中向ReentrantLock，CountDownLatch 内部实现都依赖与AbstractQueuedSynchronizer（用于控制同步的框架AQS），下面我们来分析独占模式下的原理</p>
<h2 id="原理"><a href="#原理" class="headerlink" title="原理"></a>原理</h2><p>队列同步器AQS是用来构建锁和其他同步组件的基础框架，内部使用int来表示成员的同步状态，通过内置的FIFO队列来完成资源获取线程的排序工作，其中成员变量包括 内部状态state 、等待队列的对头head(对头是一个空节点,也可以认为是当前持有锁的线程)、等待队列的队尾tail，都是通过volatile修饰，保证在并发过程中对其他线程可见</p>
<p>基本结构：<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">public</span> <span class="keyword">abstract</span> <span class="class"><span class="keyword">class</span> <span class="title">AbstractQueuedSynchronizer</span></span></div><div class="line"><span class="class">    <span class="keyword">extends</span> <span class="title">AbstractOwnableSynchronizer</span></span></div><div class="line"><span class="class">    <span class="keyword">implements</span> <span class="title">java</span>.<span class="title">io</span>.<span class="title">Serializable</span> </span>&#123;</div><div class="line">    <span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node head;</div><div class="line"></div><div class="line">    <span class="keyword">private</span> <span class="keyword">transient</span> <span class="keyword">volatile</span> Node tail;</div><div class="line"></div><div class="line">    <span class="comment">/**</span></div><div class="line"><span class="comment">     * 同步状态</span></div><div class="line"><span class="comment">     */</span></div><div class="line">    <span class="keyword">private</span> <span class="keyword">volatile</span> <span class="keyword">int</span> state;</div><div class="line"></div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<p>AQS内部队列的实现原理<br><figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div></pre></td><td class="code"><pre><div class="line"><span class="keyword">static</span> <span class="keyword">final</span> <span class="class"><span class="keyword">class</span> <span class="title">Node</span> </span>&#123;</div><div class="line">        <span class="comment">/** Marker to indicate a node is waiting in shared mode */</span></div><div class="line">        <span class="keyword">static</span> <span class="keyword">final</span> Node SHARED = <span class="keyword">new</span> Node();</div><div class="line">        <span class="comment">/** Marker to indicate a node is waiting in exclusive mode */</span></div><div class="line">        <span class="keyword">static</span> <span class="keyword">final</span> Node EXCLUSIVE = <span class="keyword">null</span>;</div><div class="line"></div><div class="line">        <span class="comment">/** waitStatus value to indicate thread has cancelled */</span></div><div class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> CANCELLED =  <span class="number">1</span>;</div><div class="line">        <span class="comment">/** waitStatus value to indicate successor's thread needs unparking */</span></div><div class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> SIGNAL    = -<span class="number">1</span>;</div><div class="line">        <span class="comment">/** waitStatus value to indicate thread is waiting on condition */</span></div><div class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> CONDITION = -<span class="number">2</span>;</div><div class="line">        <span class="comment">/**</span></div><div class="line"><span class="comment">         * waitStatus value to indicate the next acquireShared should</span></div><div class="line"><span class="comment">         * unconditionally propagate</span></div><div class="line"><span class="comment">         */</span></div><div class="line">        <span class="keyword">static</span> <span class="keyword">final</span> <span class="keyword">int</span> PROPAGATE = -<span class="number">3</span>;</div><div class="line"></div><div class="line">        <span class="keyword">volatile</span> <span class="keyword">int</span> waitStatus;</div><div class="line">        <span class="keyword">volatile</span> Node prev;</div><div class="line">        <span class="keyword">volatile</span> Node next;</div><div class="line">        <span class="keyword">volatile</span> Thread thread;</div><div class="line">        Node nextWaiter;</div></pre></td></tr></table></figure></p>
<ul>
<li><p>每当有线程竞争锁失败，那么会将其加入到队列的队尾，tail始终指向最后一个元素</p>
</li>
<li><p>节点存储的信息包括 节点的模式，前驱和后继节点，当前线程引用，以及当前状态waitStatus,<br>多线程并发时，会存在多个线程节点，waitStatus表示当前节点的状态，有的线程可能在等待过程中放弃，有的节点可能在等待条件满足后在触发。状态分为以下四种：</p>
<ul>
<li>CANCELLED 取消状态</li>
<li>SIGNAL 等待触发状态</li>
<li>CONDITION 等待条件状态</li>
<li>PROPAGATE 状态需要向后传播</li>
</ul>
</li>
<li><p>等待队列是FIFO先进先出，只有前一个节点的状态为SIGNAL时，当前节点的线程才能被挂起。</p>
</li>
</ul>
<h2 id="源码解析："><a href="#源码解析：" class="headerlink" title="源码解析："></a>源码解析：</h2><ol>
<li>acquire(int)</li>
</ol>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div></pre></td><td class="code"><pre><div class="line"></div><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">void</span> <span class="title">acquire</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</div><div class="line">     <span class="keyword">if</span> (!tryAcquire(arg) &amp;&amp;</div><div class="line">         acquireQueued(addWaiter(Node.EXCLUSIVE), arg))</div><div class="line">         selfInterrupt();</div><div class="line"> &#125;</div><div class="line"> <span class="comment">/** 需要有子类进行实现 **/</span></div><div class="line"> <span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">tryAcquire</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</div><div class="line">     <span class="keyword">throw</span> <span class="keyword">new</span> UnsupportedOperationException();</div><div class="line"> &#125;</div></pre></td></tr></table></figure>
<pre><code>1. tryAcquire 尝试获取锁资源，如果获取成功，直接返回
2. 首先通过addWaiter 以独占模式将当前线程添加到队尾，而后accquireQueued 在队列中获取资源，直到获取到资源,如果在获取过程中被中断，则返回true，否则返回false
3. 线程在等待过程中被中断过，它是不响应的。只是获取资源后才再进行自我中断selfInterrupt()，将中断补上
</code></pre><ol>
<li>addWaiter(Node)</li>
</ol>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div></pre></td><td class="code"><pre><div class="line"></div><div class="line"><span class="function"><span class="keyword">private</span> Node <span class="title">addWaiter</span><span class="params">(Node mode)</span> </span>&#123;</div><div class="line">    Node node = <span class="keyword">new</span> Node(Thread.currentThread(), mode);</div><div class="line">    <span class="comment">// Try the fast path of enq; backup to full enq on failure</span></div><div class="line">    Node pred = tail;</div><div class="line">    <span class="keyword">if</span> (pred != <span class="keyword">null</span>) &#123;</div><div class="line">        node.prev = pred;</div><div class="line">        <span class="keyword">if</span> (compareAndSetTail(pred, node)) &#123;</div><div class="line">            pred.next = node;</div><div class="line">            <span class="keyword">return</span> node;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    enq(node);</div><div class="line">    <span class="keyword">return</span> node;</div><div class="line">&#125;</div><div class="line"><span class="function"><span class="keyword">private</span> Node <span class="title">enq</span><span class="params">(<span class="keyword">final</span> Node node)</span> </span>&#123;</div><div class="line">    <span class="keyword">for</span> (;;) &#123;</div><div class="line">        Node t = tail;</div><div class="line">        <span class="keyword">if</span> (t == <span class="keyword">null</span>) &#123; <span class="comment">// Must initialize</span></div><div class="line">            <span class="keyword">if</span> (compareAndSetHead(<span class="keyword">new</span> Node()))</div><div class="line">                tail = head;</div><div class="line">        &#125; <span class="keyword">else</span> &#123;</div><div class="line">            node.prev = t;</div><div class="line">            <span class="keyword">if</span> (compareAndSetTail(t, node)) &#123;</div><div class="line">                t.next = node;</div><div class="line">                <span class="keyword">return</span> t;</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<pre><code>1. 首先尝试将节点直接加入队列，如果在加入过程中队尾不为空，其没有变化，那么直接加入并返回
2. 如果队尾为空，或者在加入过程中队尾指针变化，那么需要重新检测并入队。通过一个自循环，不停的检测，直到加入成功。首先获取队列队尾，检查队尾是否为空，如果为空则代表没有其他等待节点，那么首先初始化队列，将对象头置为不包含任何数据的节点，并同步队尾指针然后再次循环检查
3. 如果队尾节点不为空，那么尝试将节点添加到队尾
</code></pre><ol>
<li>acquireQueued(node, int)</li>
</ol>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div></pre></td><td class="code"><pre><div class="line"></div><div class="line"><span class="function"><span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">acquireQueued</span><span class="params">(<span class="keyword">final</span> Node node, <span class="keyword">int</span> arg)</span> </span>&#123;</div><div class="line">    <span class="keyword">boolean</span> failed = <span class="keyword">true</span>;</div><div class="line">    <span class="keyword">try</span> &#123;</div><div class="line">        <span class="keyword">boolean</span> interrupted = <span class="keyword">false</span>;</div><div class="line">        <span class="keyword">for</span> (;;) &#123;</div><div class="line">            <span class="keyword">final</span> Node p = node.predecessor();</div><div class="line">            <span class="keyword">if</span> (p == head &amp;&amp; tryAcquire(arg)) &#123;</div><div class="line">                setHead(node);</div><div class="line">                p.next = <span class="keyword">null</span>; <span class="comment">// help GC</span></div><div class="line">                failed = <span class="keyword">false</span>;</div><div class="line">                <span class="keyword">return</span> interrupted;</div><div class="line">            &#125;</div><div class="line">            <span class="keyword">if</span> (shouldParkAfterFailedAcquire(p, node) &amp;&amp;</div><div class="line">                parkAndCheckInterrupt())</div><div class="line">                interrupted = <span class="keyword">true</span>;</div><div class="line">        &#125;</div><div class="line">    &#125; <span class="keyword">finally</span> &#123;</div><div class="line">        <span class="keyword">if</span> (failed)</div><div class="line">            cancelAcquire(node);</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<pre><code>进入等待状态休息，直到其他线程彻底释放资源后唤醒自己，自己再拿到资源，然后就可以去干自己想干的事了。
1. 检查当前节点的前驱节点，如果前驱节点是头节点，那么更新头节点，并返回是否在获取过程中被中断(在addWaiter中如果队列为空那么会先初始化一个头节点，然后将节点添加进去，在这一步又使用节点替换头节点)
2. 如果前驱非头节点，那么检查是否需要检查等待状态然后等待被唤醒并检查是否在等待过程中被中断
</code></pre><ol>
<li>shouldParkAfterFailedAcquire(node,node)</li>
</ol>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div><div class="line">31</div><div class="line">32</div><div class="line">33</div><div class="line">34</div><div class="line">35</div><div class="line">36</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">boolean</span> <span class="title">shouldParkAfterFailedAcquire</span><span class="params">(Node pred, Node node)</span> </span>&#123;</div><div class="line">    <span class="keyword">int</span> ws = pred.waitStatus;</div><div class="line">    <span class="keyword">if</span> (ws == Node.SIGNAL)</div><div class="line">        <span class="comment">/*</span></div><div class="line"><span class="comment">         * This node has already set status asking a release</span></div><div class="line"><span class="comment">         * to signal it, so it can safely park.</span></div><div class="line"><span class="comment">         */</span></div><div class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</div><div class="line">    <span class="keyword">if</span> (ws &gt; <span class="number">0</span>) &#123;</div><div class="line">        <span class="comment">/*</span></div><div class="line"><span class="comment">         * Predecessor was cancelled. Skip over predecessors and</span></div><div class="line"><span class="comment">         * indicate retry.</span></div><div class="line"><span class="comment">         */</span></div><div class="line">        <span class="keyword">do</span> &#123;</div><div class="line">            node.prev = pred = pred.prev;</div><div class="line">        &#125; <span class="keyword">while</span> (pred.waitStatus &gt; <span class="number">0</span>);</div><div class="line">        pred.next = node;</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        <span class="comment">/*</span></div><div class="line"><span class="comment">         * waitStatus must be 0 or PROPAGATE.  Indicate that we</span></div><div class="line"><span class="comment">         * need a signal, but don't park yet.  Caller will need to</span></div><div class="line"><span class="comment">         * retry to make sure it cannot acquire before parking.</span></div><div class="line"><span class="comment">         */</span></div><div class="line">        compareAndSetWaitStatus(pred, ws, Node.SIGNAL);</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">parkAndCheckInterrupt</span><span class="params">()</span> </span>&#123;</div><div class="line">    LockSupport.park(<span class="keyword">this</span>);</div><div class="line">    <span class="keyword">return</span> Thread.interrupted();</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">selfInterrupt</span><span class="params">()</span> </span>&#123;</div><div class="line">    Thread.currentThread().interrupt();</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<pre><code>1. 检查上一个节点的状态是否是可唤醒状态，如果是则返回true，并执行parkAndCheckInterrupt进行park
2. 如果不是，那么针对于状态大于0的都是取消状态，需要忽略，那么从队尾指针往前找直到某个状态小于等于0，那节点的前驱节点设置为找寻到的节点，然后进行新一轮的自旋迭代

3. 如果不是可唤醒状态，而且状态不大于0，那么需要修改节点状态，再一次自旋
</code></pre><p>结合aquireQueued，流程如下：</p>
<pre><code>1. 结点进入队尾后，检查状态，找到安全休息点；
2. 调用park()进入waiting状态，等待unpark()或interrupt()唤醒自己；
3. 被唤醒后，看自己是不是有资格能拿到号。如果拿到，head指向当前结点，并返回从入队到拿到号的整个过程中是否被中断过；如果没拿到，继续流程1。
</code></pre><ol>
<li>release(int)</li>
</ol>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div><div class="line">12</div><div class="line">13</div><div class="line">14</div><div class="line">15</div><div class="line">16</div><div class="line">17</div><div class="line">18</div><div class="line">19</div><div class="line">20</div><div class="line">21</div><div class="line">22</div><div class="line">23</div><div class="line">24</div><div class="line">25</div><div class="line">26</div><div class="line">27</div><div class="line">28</div><div class="line">29</div><div class="line">30</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">final</span> <span class="keyword">boolean</span> <span class="title">release</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</div><div class="line">    <span class="keyword">if</span> (tryRelease(arg)) &#123;</div><div class="line">        Node h = head;</div><div class="line">        <span class="keyword">if</span> (h != <span class="keyword">null</span> &amp;&amp; h.waitStatus != <span class="number">0</span>)</div><div class="line">            unparkSuccessor(h);</div><div class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> <span class="keyword">false</span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="comment">/** 由子类实现 **/</span></div><div class="line"><span class="function"><span class="keyword">protected</span> <span class="keyword">boolean</span> <span class="title">tryRelease</span><span class="params">(<span class="keyword">int</span> arg)</span> </span>&#123;</div><div class="line">    <span class="keyword">throw</span> <span class="keyword">new</span> UnsupportedOperationException();</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title">unparkSuccessor</span><span class="params">(Node node)</span> </span>&#123;</div><div class="line">    <span class="keyword">int</span> ws = node.waitStatus;</div><div class="line">    <span class="keyword">if</span> (ws &lt; <span class="number">0</span>)</div><div class="line">        compareAndSetWaitStatus(node, ws, <span class="number">0</span>);</div><div class="line"></div><div class="line">    Node s = node.next;</div><div class="line">    <span class="keyword">if</span> (s == <span class="keyword">null</span> || s.waitStatus &gt; <span class="number">0</span>) &#123;</div><div class="line">        s = <span class="keyword">null</span>;</div><div class="line">        <span class="keyword">for</span> (Node t = tail; t != <span class="keyword">null</span> &amp;&amp; t != node; t = t.prev)</div><div class="line">            <span class="keyword">if</span> (t.waitStatus &lt;= <span class="number">0</span>)</div><div class="line">                s = t;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">if</span> (s != <span class="keyword">null</span>)</div><div class="line">        LockSupport.unpark(s.thread);<span class="comment">//唤醒</span></div><div class="line">&#125;</div></pre></td></tr></table></figure>
<pre><code>1. 根据tryRelease 查看是否释放成功
2. 如果释放成功，那么对线程进行unpark唤起 head节点一般是当前线程所在节点，首先查看head节点的状态，如果状态小于0，那么将其置为0，然后遍历查找下一个需要唤醒的节点，如果节点的状态大于0，表明该节点已经取消，那么需要从队尾遍历往前找到最头不为且状态不大于0的节点进行唤醒
</code></pre><p>总结：</p>
<p>以上就是AQS对于独占模式下线程竞争的数据处理过程,下节分析共享模式下的处理流程</p>
]]></content>
      
        
        <tags>
            
            <tag> java并发 </tag>
            
            <tag> concurrent </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[统计字节数组中二进制1的数量]]></title>
      <url>/2017/12/21/swar/</url>
      <content type="html"><![CDATA[<p>统计字节数组中 <code>1</code> 的数量, 常用的算法有如下几种：</p>
<p>1). <strong>遍历算法</strong></p>
<p>顾名思义， 遍历二进制位中的每一位，如果该位为1，那么数量加1<br>好处： 方法简单直接<br>坏处： 效率较低， 例如检测大小100M的字节数组（1MB = 1024KB = 1048576B = 8388608byte)，那么<br>需要进行大于8亿次的检查</p>
<p>2). <strong>查表算法</strong></p>
<p>针对于遍历算法的一个优化， 一般情况下， 对于一个特定的有限集合来说，集合元素的排列方式是有限的，对于一个有限长度的位数组来说， 他表示的二进制位的排列也是有限的</p>
<p>我们可以针对8位的二进制定制查表2^8,那么针对与100M数据来说，需要查询大约1亿次表<br>如果我们将8位提升到16位，那么表数量位2^16,(需要几百KB的字节空间), 查表所需次数为五千万次;<br>如果我们将16位提升到32位，那么表数量位2^32(需要十多个G的空间)，查询表数位两千五百万次</p>
<p>因此这也算是一种空间换时间的方式，但空间的消耗在32位时已到达一个难以忍受的地步；另外由于CPU的缓存限制，创建的表越大，那么CPU缓存所保存的内容占比就越小，从而缓存不明中率提高，也直接影响了算法的性能</p>
<p>3). <strong>variable-percision swar</strong></p>
<p><code>汉明重量</code> 通过利用位移和位运算，可以在常数时间内计算多个字节的汉明重量，并且不需要额外的内存。</p>
<figure class="highlight java"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div><div class="line">9</div><div class="line">10</div><div class="line">11</div></pre></td><td class="code"><pre><div class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">int</span> <span class="title">swar</span><span class="params">(<span class="keyword">int</span> i)</span> </span>&#123;</div><div class="line">   i = (i &amp; <span class="number">0x55555555</span>) + ((i &gt;&gt; <span class="number">1</span>) &amp; <span class="number">0x555555555</span>; </div><div class="line"></div><div class="line">   i = (i &amp; <span class="number">0x33333333</span>) + ((i &gt;&gt; <span class="number">2</span>) &amp; <span class="number">0x33333333</span>);</div><div class="line"></div><div class="line">   i = (i &amp; <span class="number">0x0F0F0F0F</span>) + ((i &gt;&gt; <span class="number">4</span>) &amp; <span class="number">0x0F0F0F0F</span>);</div><div class="line"></div><div class="line">   i = (i * <span class="number">0x01010101</span>) &gt;&gt; <span class="number">24</span>;</div><div class="line"></div><div class="line">   <span class="keyword">return</span> i;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>我们以980480539(0x3A70F21B)作为计算数，</p>
<p>第一步计算，按两位为一组</p>
<table>
<thead>
<tr>
<th style="text-align:left">值</th>
<th style="text-align:left">分组</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">0x3A70F21B</td>
<td style="text-align:left">00 11 10 10 01 11 00 00 11 11 00 10 00 01 10 11</td>
</tr>
<tr>
<td style="text-align:left">0x2560A116</td>
<td style="text-align:left">00 10 01 01 01 10 00 00 10 10 00 01 00 01 01 10</td>
</tr>
<tr>
<td style="text-align:left">汉明重量</td>
<td style="text-align:left">0  2  1  1  1  2  0  0  2  2  0  1  0  1  1  2</td>
</tr>
</tbody>
</table>
<p>实际利用的是二进制为相加, ox55 = 0x01010101, i &amp; 0x55555555计算的是偶数位的值，然后i向右移动一位，计算的该奇数位的值，然后相加就是这两位1的个数</p>
<p>第二步按4位一组计算</p>
<table>
<thead>
<tr>
<th style="text-align:left">值</th>
<th style="text-align:left">分组</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">0x2560A116</td>
<td style="text-align:left">0010 0101 0110 0000 1010 0001 0001 0110</td>
</tr>
<tr>
<td style="text-align:left">0x22304113</td>
<td style="text-align:left">0010 0010 0011 0000 0100 0001 0001 0011</td>
</tr>
<tr>
<td style="text-align:left">汉明重量</td>
<td style="text-align:left">2    2    3    0    4    1    1    3</td>
</tr>
</tbody>
</table>
<p>第三步按8位一组计算</p>
<table>
<thead>
<tr>
<th style="text-align:left">值</th>
<th style="text-align:left">分组</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">0x22304113</td>
<td style="text-align:left">00100010 00110000 01000001 00010011</td>
</tr>
<tr>
<td style="text-align:left">0x4030504</td>
<td style="text-align:left">00000100 00000011 00000101 00000100</td>
</tr>
<tr>
<td style="text-align:left">汉明重量</td>
<td style="text-align:left">4        3        5        4</td>
</tr>
</tbody>
</table>
<p>第四步，与0x0 1010101相乘， 将位和集中到高8位</p>
<p>实际如下：<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div><div class="line">6</div><div class="line">7</div><div class="line">8</div></pre></td><td class="code"><pre><div class="line">                              00000100 00000011 00000101 00000100</div><div class="line">                      0000100 00000011 00000101 00000100</div><div class="line">            00000100 00000011 00000101 00000100</div><div class="line">   00000100 00000011 00000101 00000100</div><div class="line"> &lt;---------------------------------------------&gt;</div><div class="line">                              00010000 00001100 00001001 00000100</div><div class="line"></div><div class="line">高8位是所有的8位的汉明重量之和，结果位16</div></pre></td></tr></table></figure></p>
<p>可以一次计算32位字节，甚至我们可以在一次循环中调用4次swar，这样就可以一次处理128位字节</p>
]]></content>
      
        <categories>
            
            <category> algorithm </category>
            
        </categories>
        
        
        <tags>
            
            <tag> 汉明距离，位运算 </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[Github与TravisCi的集成]]></title>
      <url>/2017/09/24/github-with-travis-ci/</url>
      <content type="html"><![CDATA[<p>在看github上好的开源项目时，总会在README中看到类似的成功或失败的图标, 那么它们时怎么生成的呢，这就不可避免需要谈到Github的好基友<a href="https://travis-ci.org/" target="_blank" rel="external">Travis-Ci</a>, 下面就来说一下github与travis-ci的集成过程. <img src="https://travis-ci.org/lys091112/moonstar.svg?branch=master" alt="Build Status"></p>
<p><strong>简单分为一下几步</strong></p>
<ul>
<li>首先需要有一个github账号，并且需要有个自己的项目（废话，没有项目谁会去弄这东西，哈哈）</li>
<li>有github账号登录<a href="https://travis-ci.org/" target="_blank" rel="external">Travis-Ci</a></li>
<li>在Travis-Ci中，进入 <code>Accounts</code> 界面， 在该界面上选择要进行集成测试的项目(这个按照页面的说明进行)</li>
<li>需要在项目的根目录下建立 <code>.travis.yml</code> 文件，具体文件的书写语法可网上搜寻或参考开源项目</li>
<li>将代码push上去后，如果一切顺利，Travis就会自动开始构建这个Maven工程。可以在Travis上看到构建结果和详细的输出</li>
<li>如果想要添加 <code>build:passing</code> 这样的图标， 可以在REASDME.md中添加这样的一个图标  <figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">[![Build Status](https://travis-ci.org/$&#123;accountName&#125;/$&#123;projectName&#125;.svg?branch=master)](https://travis-ci.org/$&#123;accountName&#125;/$&#123;projectName&#125;)</div><div class="line"></div><div class="line">例如我的地址为：</div><div class="line">[![Build Status](https://travis-ci.org/lys091112/moonstar.svg?branch=master)](https://travis-ci.org/lys091112/moonstar)</div></pre></td></tr></table></figure>
</li>
</ul>
]]></content>
      
        <categories>
            
            <category> util </category>
            
        </categories>
        
        
        <tags>
            
            <tag> github </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[利用Hexo搭建个人博客]]></title>
      <url>/2017/08/06/hexo-blog/</url>
      <content type="html"><![CDATA[<p>首先要申请个人github账号，并以固定格式建立项目名称: {userName}.github.io, 这是github-pages 要求的项目名称。 </p>
<p>本次安装针对于mac系统，linux类似。</p>
<h2 id="1-搭建"><a href="#1-搭建" class="headerlink" title="1. 搭建"></a>1. 搭建</h2><h3 id="1-1-依赖程序安装"><a href="#1-1-依赖程序安装" class="headerlink" title="1.1. 依赖程序安装"></a>1.1. 依赖程序安装</h3><ol>
<li><p>安装node<br>在控制台下执行<code>brew install node</code></p>
</li>
<li><p>安装hexo<br>执行命令<code>npm install -g hexo</code></p>
</li>
<li><p>由于在<code>hexo 3.x</code> 版本之后, <code>hexo-server</code> 模块单独出来，需要独立安装<br> 执行命令 <code>npm install hexo-server --save</code>, 可以通过 <code>hexo server</code>命令启动 Hexo，访问如出现类似 <strong>can’t not GET</strong> 等错误，执行<code>npm install</code></p>
</li>
<li><p>安装 git 模块，使hexo可以推送到git<br>执行命令 <code>npm install hexo-deployer-git --save</code></p>
</li>
</ol>
<p>至此，依赖安装完毕。</p>
<h3 id="1-2-开发流程"><a href="#1-2-开发流程" class="headerlink" title="1.2. 开发流程"></a>1.2. 开发流程</h3><ol>
<li><p><code>git clone {own-repo}</code> 到本地. </p>
</li>
<li><p>建立博客目录,(例如： <code>mkdir blog</code>), 然后进入该目录，执行 <code>hexo init</code>， 此时目录会自动生成hexo的基础文件，编辑 <code>_config.xml</code> 文件, 修改如下部分</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div></pre></td><td class="code"><pre><div class="line">deploy:</div><div class="line">  type: git</div><div class="line">  repo: git@github.com:&lt;userName&gt;/&lt;userName&gt;.github.io.git</div><div class="line">  branch: master</div></pre></td></tr></table></figure>
<p>为了避免更换电脑或系统之后，<code>blog</code> 文件的丢失，可以将其push 到github上进行保存。 方式： 建立分支<code>git checkout -b hexo</code>, 然后将文件push 到 github上。 </p>
</li>
<li><p>书写文档，并发布到github。 常用命令如下：</p>
</li>
</ol>
<ul>
<li>hexo g(enerate): 生成静态文件，在public文件夹中</li>
<li>hexo s(erver): 生成本地预览，默认情况下，网址为： <a href="http://localhost:4000/" target="_blank" rel="external">http://localhost:4000/</a></li>
<li>hexo d(eploy): 部署并提交代码至GitHub中</li>
<li>hexo clean：用于清除缓存文件和已经生成的静态文件， 如果发现自己的修改对站点不生效， 那么可以尝试运行该命令</li>
</ul>
<h3 id="1-3-主体更换"><a href="#1-3-主体更换" class="headerlink" title="1.3. 主体更换"></a>1.3. 主体更换</h3><p>常用主题<a href="https://github.com/iissnan/hexo-theme-next" target="_blank" rel="external">Next</a></p>
<p><code>cd {your-own-blog}</code>, 然后进入 <code>themes</code> 目录，将<a href="https://github.com/iissnan/hexo-theme-next" target="_blank" rel="external">Next</a> 克隆到该目录下，<br>然后修改<code>_config.xml</code>文件如下:<br><figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">theme: hexo-theme-next</div></pre></td></tr></table></figure></p>
<h4 id="1-3-1-Next-使用"><a href="#1-3-1-Next-使用" class="headerlink" title="1.3.1. Next 使用"></a>1.3.1. Next 使用</h4><ol>
<li><p>修改语言设置</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">在站点配置文件 _config.yml, 修改language: zh-Hans</div></pre></td></tr></table></figure>
</li>
<li><p>添加 <code>about</code> 页面。</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line">修改主题配置文件 ``_config.yml`` 文件，打开 ``menu`` 下的 about 标签，</div><div class="line">执行 ``hexo new page about`` ,创建``about`` 页面</div></pre></td></tr></table></figure>
</li>
<li><p>添加 tag 页面</p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div></pre></td><td class="code"><pre><div class="line">修改主题配置文件 ``_config.yml`` 文件，打开 ``menu`` 下的 about 标签，</div><div class="line">执行 ``hexo new page tags`` ,创建 ``tags``  页面</div><div class="line">编辑新产生的 tags 页面，修改添加 ``type：tags``</div></pre></td></tr></table></figure>
</li>
</ol>
<h2 id="2-插件使用"><a href="#2-插件使用" class="headerlink" title="2. 插件使用"></a>2. 插件使用</h2><h3 id="2-1-搜索插件"><a href="#2-1-搜索插件" class="headerlink" title="2.1 搜索插件"></a>2.1 搜索插件</h3><ol>
<li><p><strong>安装 hexo-generator-search</strong></p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">npm install hexo-generator-search --save</div></pre></td></tr></table></figure>
</li>
<li><p><strong>安装 hexo-generator-searchdb</strong></p>
<figure class="highlight plain"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">npm install hexo-generator-searchdb --save</div></pre></td></tr></table></figure>
</li>
<li><p><strong>修改站点目录下文件 <code>_config.xml</code></strong></p>
<figure class="highlight yml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div><div class="line">3</div><div class="line">4</div><div class="line">5</div></pre></td><td class="code"><pre><div class="line"><span class="attr">search:</span></div><div class="line"><span class="attr">  path:</span> <span class="string">search.xml</span></div><div class="line"><span class="attr">  field:</span> <span class="string">post</span></div><div class="line"><span class="attr">  format:</span> <span class="string">html</span></div><div class="line"><span class="attr">  limit:</span> <span class="number">10000</span></div></pre></td></tr></table></figure>
</li>
<li><p><strong>修改主题配置文件 <code>_config.xml</code> </strong></p>
<figure class="highlight yml"><table><tr><td class="gutter"><pre><div class="line">1</div><div class="line">2</div></pre></td><td class="code"><pre><div class="line"><span class="attr">local_search:</span></div><div class="line"><span class="attr">  enable:</span> <span class="literal">true</span></div></pre></td></tr></table></figure>
</li>
</ol>
]]></content>
      
        <categories>
            
            <category> util </category>
            
        </categories>
        
        
        <tags>
            
            <tag> hexo </tag>
            
        </tags>
        
    </entry>
    
    <entry>
      <title><![CDATA[First Step]]></title>
      <url>/2017/08/01/hello-world/</url>
      <content type="html"><![CDATA[<p>Welcome to <a href="https://hexo.io/" target="_blank" rel="external">Hexo</a>! This is your very first post. Check <a href="https://hexo.io/docs/" target="_blank" rel="external">documentation</a> for more info. If you get any problems when using Hexo, you can find the answer in <a href="https://hexo.io/docs/troubleshooting.html" target="_blank" rel="external">troubleshooting</a> or you can ask me on <a href="https://github.com/hexojs/hexo/issues" target="_blank" rel="external">GitHub</a>.</p>
<h2 id="Quick-Start"><a href="#Quick-Start" class="headerlink" title="Quick Start"></a>Quick Start</h2><h3 id="Create-a-new-post"><a href="#Create-a-new-post" class="headerlink" title="Create a new post"></a>Create a new post</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ hexo new <span class="string">"My New Post"</span></div></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/writing.html" target="_blank" rel="external">Writing</a></p>
<h3 id="Run-server"><a href="#Run-server" class="headerlink" title="Run server"></a>Run server</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ hexo server</div></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/server.html" target="_blank" rel="external">Server</a></p>
<h3 id="Generate-static-files"><a href="#Generate-static-files" class="headerlink" title="Generate static files"></a>Generate static files</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ hexo generate</div></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/generating.html" target="_blank" rel="external">Generating</a></p>
<h3 id="Deploy-to-remote-sites"><a href="#Deploy-to-remote-sites" class="headerlink" title="Deploy to remote sites"></a>Deploy to remote sites</h3><figure class="highlight bash"><table><tr><td class="gutter"><pre><div class="line">1</div></pre></td><td class="code"><pre><div class="line">$ hexo deploy</div></pre></td></tr></table></figure>
<p>More info: <a href="https://hexo.io/docs/deployment.html" target="_blank" rel="external">Deployment</a></p>
]]></content>
      
        
    </entry>
    
  
  
</search>
